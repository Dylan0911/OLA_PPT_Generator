# 天主教弥撒双语 PPT 自动生成工具 — 交接文档

> 目的:把一份中英双语的天主教弥撒礼仪 Word 文档,自动生成成一份和"人工手工排版"几乎一致的 PPTX。
> 本文档给接手的 AI/工程师,写清楚**做了什么、怎么运行、所有必须避免的坑、以及下一步要提高的泛化能力**。

---

## 0. TL;DR(先读这段)

- 语言/依赖:**纯 Node.js**(无框架)+ 少量 Python(仅"对比报告"辅助工具)。PPTX 全靠**解压 XML → 正则改写 → 重打包**,没用任何 pptx 库。
- 核心两步:**① 原型模板**(填空模板,占位符)+ **② 填充程序**(把每周文档内容填进去、分页、排版)。
- 目前只在**常年期(Ordinary Time)第十二、十三、十五主日**验证过。用户最关心的下一步 = **提高泛化**(换任意主日/节期都不崩不错)。
- ⚠️ **改任何东西后,必须重新生成并逐项验证**(XML 有效性 + 无残留 `{{}}` + 无文字溢出 + 和人工版对比),用户对"改一个地方弄坏另一个"极度敏感。
- ⚠️ 不要动 `scripts/build-liturgy-ppt.mjs`(已废弃的旧生成器,和现方案无关)。

---

## 1. 环境与运行

- 项目根目录:
  `/Users/dylan/Downloads/OLA PPT/local-ppt-tool/`
- 启动网页工具:
  ```bash
  cd "…/local-ppt-tool"
  node server.mjs        # 看到 "Liturgy PPT tool running at http://127.0.0.1:4177"
  ```
  浏览器打开 `http://127.0.0.1:4177` → 上传 .docx/.txt → 下载成品 PPTX。
- 命令行直接生成(调试常用):
  ```bash
  node scripts/build-from-template.mjs <模板.pptx> <工作目录> <输出.pptx> <sections.json>
  # 例:
  node scripts/build-from-template.mjs assets/常年期原型模板_命名占位符.pptx /tmp/w out.pptx runs/xxx/sections.json
  ```

---

## 2. 整体架构(两步)

### 步骤 ①:原型模板 `assets/常年期原型模板_命名占位符.pptx`
一份"填空模板":
- 每个**可变段落**(集祷经/读经一/读经二/答唱咏/福音前欢呼/福音/信友祷文/献礼经/领圣体后经/领主咏)只留**一张原型页**,里面是**命名占位符** `{{读经一_CN}}` `{{读经一_EN}}` `{{主日标题_CN}}` 等。
- **固定经文**(致候礼/忏悔礼/光荣颂/信经/8 篇颂谢词/感恩经一~四/天主经/平安礼/羔羊颂/降福/圣弥额尔祷文…)保留**真实文字**。
- **歌咏/堂区报告**(进堂咏/奉献咏/领主咏/礼成咏 + 堂区报告)是**红字「请手动填入」**(这些每周不同、且不在 Word 文档里,靠人工填)。
- 形状 id 约定(绿色 OLA deck):**id2 = 标题栏、id3 = 中文、id4 = 英文**;封面标题在 id4;歌咏页标题在 id10、歌词框在 id8。

生成器 = `scripts/make-prototype-template.mjs`(从一份"成品 deck"折叠删页 + 命名占位符而来)。
⚠️ **折叠删页时会重新编号,但不修跳转按钮的 target → 模板里的跳转 target 已经全部失效**(填充程序里靠"按钮文字"重新指向,见 §5)。

### 步骤 ②:填充程序 `scripts/build-from-template.mjs`(主脚本,~1300 行)
1. `server.mjs` 把 Word 文档解析成 `sections.json`。
2. 本脚本加载原型模板,对每个占位符/固定页:按比例中英配对、克隆原型页分页、统一字号、渲染各种礼仪细节,最后重打包成 PPTX。

---

## 3. 数据流:`sections.json` 格式(server.mjs 产出)

```json
{
  "meta": { "titleCn": "常年期第十二主日", "titleEn": "Twelfth Sunday in Ordinary Time", "includeGloria": true, "themeColor": "" },
  "sections": [
    { "titleCn": "集祷经", "titleEn": "Collect", "cn": "…", "en": "…" },
    { "titleCn": "读经一", "titleEn": "First Reading", "cn": "恭读…\n正文…\n——上主的圣言。", "en": "…" },
    …
  ],
  "missing": ["读经二", …]
}
```

- `server.mjs` 里两张关键表:
  - `SECTION_TITLES`:文档里能识别的**段落标题**(用于把文档切成 bucket)。
  - `OUTPUT_SECTIONS`:实际**输出到 sections 数组**的段落(有序)。
  - ⚠️ **一个段落必须同时在这两张表里才会进 sections**。曾经"领主咏"只在 SECTION_TITLES、不在 OUTPUT_SECTIONS,导致文档里的领主咏内容被解析却丢弃(已修:OUTPUT_SECTIONS 加了 `["领主咏","Communion Antiphon"]`)。
- docx 文本抽取:`extractDocxText`(解压 word/document.xml,按 `<w:p>` 分段,`<w:br>`→换行)。

### 标题解析 & 成品命名(server.mjs `parseDocument` / `handleGenerate`)
- **标题**:取文档第一行中文,`parseDocument` 里清理:去「（甲年）」括注、去破折号后缀、**去结尾无括号的「甲年/乙年/丙年」**(文档常用**全角空格**分隔如 `常年期第十五主日␣␣甲年`,所以不能只靠去括号)。清理后的 `titleCn` 用于封面标题 + 文件名。
- **成品文件名** = `{titleCn}_生成版.pptx`(节期/主日与文档一致),如 `常年期第十五主日_生成版.pptx`。`handleGenerate` 里 `titleForName` 再去一次括注 + 文件系统非法字符,解析失败兜底叫「弥撒」。下载 header 已处理中文名(ASCII 回退 + `filename*=UTF-8`)。内部 run 目录仍用时间戳,所以同一主日多次生成不会互相覆盖。

---

## 4. 填充程序:各段落处理规则(在 build-from-template.mjs)

| 段落 | 处理函数/逻辑 | 说明 |
|---|---|---|
| 封面标题 | 直接替换 `{{主日标题_CN/EN}}` | 长标题自动降号防溢出 |
| 读经一/读经二/福音 | `peelReadingIntro` + 流式分页 | 出处「恭读…↔A reading from…」去章节数字后单独成页;结尾「——上主的圣言/答:感谢天主」(福音是「基督的福音/基督,我们赞美你」)加粗独立页;福音第一页 = 固定对话(愿主与你们同在…)硬编码,只有出处来自文档 |
| 答唱咏 | `psalmPairs` | 丢开头「咏89:2-3」出处行;答句页中英**加粗**、领句按诗节分页 |
| 福音前欢呼 | `acclaimPairs` | 丢「阿肋路亚 若14:23」出处;答句「阿肋路亚」加粗;整段合一页 |
| 集祷经/献礼经/领圣体后经 | `prayerPairs` | 正文页 + 「以上所求…/答:阿们(粗)」结礼页;集祷经正文加「请大家祈祷:」;领圣体后经首页加「请大家祈祷:/Let us pray.」 |
| 信友祷文 | 主循环 SINGLE_FILL 分支 | 一段(主祭/领)一页;**只加粗前缀**「主祭:/主礼:/领:」(run 级混排),后面正文不粗;每个"领:"后插一页「答:求主俯听我们。」(60pt 居中加粗,若文档已含答句则不补) |
| 领主咏 | 主循环 passthrough 分支(见下) | **两部分**:antiphon(领圣体咏,文档里有、每周不同)+ 歌词(红字手动填)。在红字歌词页**前**插一页 antiphon 中英内容,`dropAntiphonRef` 去掉开头经卷出处(咏34:9/若6:56 内联或独立行都能去) |
| 歌咏/堂区报告 | 红字「请手动填入」 | 不在文档里 |
| 固定经文页 | `normalizeFixedEntries`/`flushFixedBuf` | 保留真实文字,只统一字号/对齐/字距;散文页整段重排装满;带粗体/空行/诗体的页(信经/光荣颂/羔羊颂)保结构;超容才烤缩放 |

### 分页 & 排版核心
- 全卷统一字号:**中文 4800(48pt)两端对齐 / 英文 2800(28pt)左对齐**;段落标题统一 **3200(32pt)**(`unifyTitle`,去掉各页不一致的 fontScale/autofit,解决"颂谢词四~七偏小、感恩经三溢出")。`unifyTitle` 有几个**例外**:大内容框(圣圣圣幽灵形状)、姿势药丸(站立/坐下,保 24pt)、**堂区报告标题(强制 6000=60pt,用户指定中英都大)**。
- 分页 = `splitByProportion`(**DP 最优均分**,最小化各组与目标的方差、每组 ≤ 页容量)+ `alignPair`(中英同步分页,页页内容对应)+ `mergeSmallPairs`(贪心合并,页面装满)。
- **不缩字优先**:页容量按 48/28 的自然容量算,放不下就多分一页;只有"结构保真页"(信经等,不能拆)才用 `fitScaleForLines` 烤 fontScale 兜底。
- 溢出估算模型:em 单位(中日韩=1、其它=0.55em),行高 中1.18/英1.15,文本框尺寸从 layout 的 ph 解析(`resolveShapeBox`)。

---

## 5. 几个"骨架"级机制(容易被误解)

1. **页序按 presentation.xml 的 sldIdLst,不是文件名顺序**。模板手术(插页)会让文件编号最大但位置在中间;填充程序读页序必须走 sldIdLst。
2. **跳转按钮(圣圣圣/圣三颂/颂谢词)重定向**:模板里的跳转 target 已失效,**不能信**。用 `findJumpTarget(按钮文字)` 在输出页里按内容签名(id2/id10 标题 + id8/id3 正文)找匹配页,重写该 rId 的 rels target。「继续」「祷文」这种 <3 字的泛按钮跳过不改(易误配)。
3. **坐下标签**:集祷经末页右下角要有「坐下」。`injectSit` 从模板里找一个坐下形状(pill+图标)克隆注入,图标 rels 换新 id。
4. **信经「(鞠躬)」**:run 级混排——「(鞠躬)」这个 run 要小字号(2800)且不加粗;`paraRunsOf` 判断段落是否加粗时**排除**舞台指示 run(STAGE_RUN_RE),否则整段被带粗。

---

## 6. ⚠️ 泛化风险 & 硬编码假设(下一步重点)

现在很多"每周会变的东西"是硬编码/正则,换一周文档可能出错。**这是用户当前最关心的**。

**A. 固定礼仪文本(安全,每周一样,可硬编码):**
福音对话「愿主与你们同在/答:也与你的心灵同在/答:主,愿光荣归于你」、读经结尾「——上主的圣言/答:感谢天主」、福音结尾「——基督的福音/基督,我们赞美你」、「请大家祈祷/Let us pray」、「答:阿们/R. Amen」—— 这些礼仪固定,硬编码没问题。

**B. 每周会变(高风险,需要加固):**
- **信友祷文答句**:硬编码「答:求主俯听我们。」。不同堂区/主日答句不同(可能「求主垂怜我们」等)→ 应从文档探测,探不到才用默认。
- **各种出处剥离/结礼检测正则**:「恭读…」「A reading from…」「以上所求」「Through」「咏/若…出处」「答:…」—— 都是从已验证的几周文档观察的,措辞一变就漏。`dropAntiphonRef` 已尽量通用(中文经卷1-4字+章:节;英文 Cf.+书+章:节),但仍可能有没覆盖的格式。
- **答唱咏结构判断**(`psalmPairs`):靠「答:」前缀 + 英文叠句重复检测,结构不规整会回退到比例配对。
- **段落识别**靠标题关键词,是**常年期结构**;其它节期(四旬期/复活期/将临期)颂谢词、结构不同,**原型模板也是常年期专用**。
- **字号/文本框尺寸**(4800/2800、box EMU)依赖当前模板结构。

**建议做法**:用一份**结构不同的其它主日文档**压测(会不会崩/错位/溢出)→ 把 B 类硬编码逐个改成"优先从文档提取,提取不到才用默认"→ 对空段落/异常输入做健壮性处理。目标:任意常年期主日都稳,再考虑跨节期。

---

## 7. 必须避免的坑(血泪教训)

1. **`open(p,"w").write(...)` 不 close** → zip 打包时文件是 0 字节。Python 里写文件务必 `with open(...)`。
2. **缓存/字典用文件名当 key** → 生成版和人工版 slide 同名(slide2.xml),会串味。key 要带来源标识(如 zip 文件名)。曾导致对比报告左右两边完全同步、看不出差异。
3. **PowerPoint/Keynote 的 AppleScript 导出会卡死**(save-as-PNG 静默失败、AppleEvent 超时、缺字体弹窗)。别用它渲染。报告渲染一律走 **LibreOffice 无头**(`soffice --headless --convert-to pdf`)。
4. **LibreOffice 在 Mac 上没有楷体的粗体字重** → 加粗渲染出来几乎看不出(不是数据错!)。报告里靠 `prep_render_copy.py` 把加粗词临时换成**黑体**才看得见粗。**这只是报告显示问题,生成的 PPT 数据是对的,PowerPoint 里正常**。
5. **grep 在含中文的文件上时好时坏**;查东西优先用 python。
6. **中文文件名进 HTTP header 非法** → Content-Disposition 要加 ASCII 回退名(`filename*=UTF-8''…`)。
7. **正则 `<a:rPr>` 处理时,自闭合 `<a:rPr .../>` vs 带子元素 `<a:rPr>…</a:rPr>` 要分开处理**;注意别误伤 `<a:endParaRPr>`、`<a:defRPr>`。
8. 报告标注里"人工版字号=继承"没意义 → 要从 layout/master 的 ph defRPr 解析出**真实 pt**。
9. **每次改动后跑通已有几周文档确认没退化**,再交付。

---

## 8. 文件清单

**核心(生成 PPT):**
- `server.mjs` — 网页服务 + 文档解析(SECTION_TITLES/OUTPUT_SECTIONS/extractDocxText/parseDocument)。
- `scripts/build-from-template.mjs` — **主填充程序**(见 §4/§5)。
- `scripts/make-prototype-template.mjs` — 从成品 deck 生成原型模板。
- `assets/常年期原型模板_命名占位符.pptx` — **当前使用的原型模板**。

**对比报告(辅助,非核心):**
- `scripts/compare-report.sh` — 一键:渲染两份 PPT → 逐页左右对比 HTML。
- `scripts/compare_report.py` — 生成对比 HTML(按段落对齐、解析继承字号)。
- `scripts/prep_render_copy.py` — 渲染前把加粗词换黑体(仅为报告显示)。
- `scripts/make_pdf.py` — 把对比报告做成 PDF(iPhone/微信可直接看)。用法:`make_pdf.py <报告目录> <gen.pptx> <hum.pptx> <out.pdf> [标题]`。
- `scripts/embed_report.py` — 把报告文件夹打成单文件 HTML(已被 PDF 取代)。

**废弃(别动):**
- `scripts/build-liturgy-ppt.mjs`、`scripts/make-template.mjs`、`scripts/make-placeholder-template.mjs`、`assets/常年期弥撒模板_占位符版.pptx` — 早期方案的遗留。

**生成对比报告命令:**
```bash
./scripts/compare-report.sh 生成版.pptx 人工版.pptx 报告文件夹名
python3 scripts/make_pdf.py ~/Downloads/报告文件夹名 生成版.pptx 人工版.pptx ~/Downloads/输出.pdf "标题"
```

---

## 9. 验证清单(每次改完必做)

1. 重新生成 → `unzip` 检查:**所有 slide XML 有效**(`xml.dom.minidom.parse`)、**无残留 `{{}}` 占位符**。
2. **溢出检查**:按 §4 的 em 模型,逐个 id3/id4 框算 rows vs maxLines,应为 0 溢出。
3. **段落页数/内容对照人工版**(用对比报告)。
4. 关键页人工核对:福音首末页、信友祷文、答唱咏、集祷经结礼、领主咏 antiphon、堂区报告。

---

## 10. 已知待办 / 未决

- 领主咏 antiphon 页的标题栏还显示「Communion Hymn」(克隆自歌词页),严格说该是「Communion Antiphon」——待用户确认是否改。
- 信友祷文答句硬编码,应改为从文档探测(见 §6-B)。
- 跨节期(四旬期/复活期/将临期)未支持:需要对应节期的原型模板 + 颂谢词/结构适配。
- 报告里楷体加粗天然不明显(已用黑体缓解);若要 PPT 里前缀更醒目(独立行/加大/变色)是设计改动,会偏离人工版,需用户拍板。
- **按钮遮挡右下角英文(已知,用户决定暂不改)**:羔羊颂的「下跪」姿势按钮、感恩经末页的「圣三颂」跳转按钮在英文框(id4)右下角**内**(如圣三颂 y≈6070985,英文框继承 layout 到 y≈6634129),英文流到框底就被盖住。❌ **不要为此改分页逻辑**(会有回归风险,用户已明确拒绝)。曾试过"内容放得下才把英文框底缩到按钮上方(不缩字)"的方案(`clampEnBox`),已按用户要求**撤销**。若日后要做,只能是这种"缩框但绝不缩字/不动分页"的思路,且必须回归验证已有几周。

---

## 11. 交接给下一个 AI 的一句话

"这是一个把双语弥撒 Word 文档自动排版成 PPTX 的 Node 工具,架构是'原型模板 + 填充程序',已在常年期第 12/13/15/16 主日验证。§12 的任务 1(圣圣圣补 4 按钮)、任务 3(待填清单)已做完;任务 2(某些页偏稀)已查明是英文驱动的正常分页、非缺陷。请重点**提高泛化能力**(§6),别信模板里的跳转 target(§5.2),改任何东西后按 §9 清单验证、并跑通已有几周文档确认不退化(§7.9)。"

---

## 12. 🔧 任务进度(用户新提出的三项)

> 状态:**任务 1 ✅ 已完成 · 任务 3 ✅ 已完成 · 任务 2 ⚠️ 已排查,判定为「非缺陷、不宜在不缩字前提下改」**。
> 全部已在常年期第十五主日重新生成验证。改任何东西后仍按 §9 验证、跑 12/13/15/16 主日不退化。

### ✅ 任务 1:圣圣圣页补齐 4 个感恩经跳转按钮(已完成)
- **需求**:圣圣圣页右下角原本只有跳「感恩经(一)、(三)」2 个按钮;要像「请大家感谢主」页一样有 (一)(二)(三)(四) 4 个。
- **根因**:模板圣圣圣页(slide74)本身只有 2 个按钮;「请大家感谢主」页(slide43)有 4 个。且按钮是**数字图标 `<p:pic>`**(不同数字 = 不同 PNG 图),不能简单复制两份(会得到重复的「1、3」图标)。
- **实现**(`scripts/build-from-template.mjs`,重映射循环前新增一个 IIFE 克隆步骤):
  1. 找到含「请大家感谢主」且带 ≥4 个跳转 pic 的**供体页**,取其 4 个数字按钮(视觉左→右 = 一二三四)的 `<p:pic>` 块 + 各自 PNG/SVG 媒体目标(媒体在包内共享,直接引用 `../media/imageN.png`)。
  2. 对带 **2 个按钮的那张圣圣圣页**:删掉原 2 个按钮,插入克隆的 4 个(重写 `cNvPr id` / `hlinkClick r:id` / `blip r:embed` / `svgBlip r:embed` 为新 rId,并向该页 `.rels` 追加 image×2 + slide×1 关系,slide 关系先占位 `slide1.xml`)。
  3. 重映射分支改成:`pics.length===4 && body.startsWith("圣、圣、圣") → epFirst`(一~四首页);保留 `===2` 兜底。
- **验证**:第十五主日输出圣圣圣页(slide137)现有 4 按钮,左→右分别指向 感恩经(一)138 / (二)177 / (三)193 / (四)223,数字图标用的正是供体页的 1/2/3/4。另两张无按钮的圣圣圣页保持原样。
- **注意**:按钮位置沿用供体页几何(底部一排);若哪周英文框特别长,可能与按钮有轻微视觉叠压(同「按钮遮挡」已知问题,§10)。

### ✅ 任务 3:待填清单(已完成)
- **需求**:红字「请手动填入」的页(进堂咏/奉献咏/领主咏/礼成咏 + 堂区报告)不必逐页翻找。
- **实现**:
  - `build-from-template.mjs`:定稿后扫描最终 `kept`,凡含「请手动填入/请在此手动填入」的页 → `fillList.push({page, label})`(label 取 `进堂咏/奉献咏/领主咏/礼成咏/堂区报告`)。**关键**:这些红字页多为模板**静态 passthrough 页**(红字烤在模板里,不走 token 分支),所以必须在成品定稿后**整体扫描**,不能只在 token/缺段分支里记。输出 JSON 增加 `fillList`。
  - `server.mjs`:`/generate` 响应增加 `fillList`。
  - `public/index.html` + `app.js` + `styles.css`:结果面板新增「需手动填入的页」,红色药丸列出「第 X 页:进堂咏 …」。
- **验证**:第十五主日返回 5 页 — 第2页进堂咏 / 第90页奉献咏 / 第276页领主咏 / 第280页堂区报告 / 第289页礼成咏。

### ⚠️ 任务 2:部分页面不够紧凑 —— 已排查,判定「非缺陷」
- **现象**:感恩经(一)里如「晚餐后…拿在祂神圣而可敬的手中,」「又感谢赞颂祢,交给祂的门徒说:」等页,中文只有 1~2 行,看着能放更多。
- **排查结论(重要,别再当 bug 去改)**:这些页是**英文长度驱动的双语同步分页**,不是 struct 误判、也不是合并失败。
  - 例:感恩经(一)模板 slide89 是**一张原型页**,中文仅 41 字(< `twoCn.max` 50),但**英文 204 字**(> `twoEn.max` 170)。`alignPair` 按英文比例把它拆成 2 页(各 ~102 英文字),中文被同步切成 26/15 字 → 看着稀,其实**英文那栏约 6 成满**。
  - 已填满的正常 flow 页(如感恩经(一)138~151)英文都在 122~168 字 —— 说明英文栏真实容量就是 ~170 字/页。204 字**在不缩字的前提下装不进一页**。
  - **全 deck 扫描**过:找不到任何「相邻同标题、双栏都欠满、且合并后英文 ≤170」的可合并页对 —— 即所有稀页合并都会撑爆英文框。
- **为什么不改**:唯一能让它们并成一页的办法是**缩小英文字号**或**打破中英分栏同步**,两者都违反本项目既定设计(固定页超容只分页不缩字,2026-07-09 用户定的)。祝圣经文(粗体「你们大家拿去吃/喝…」)本就一句一页(正确)。
- **若将来一定要更紧凑**:这是**设计取舍题**,需用户拍板 —— 要么允许这类超长英文叙述页的英文字号略缩,要么接受中文栏偏空。**不要**默默改 `twoEn.max` 或分页逻辑(会波及所有双语页,高退化风险)。
